if __name__ == '__main__':
    pass
    # load the database (jsonl) files
# load all the files found in database directory
# import the model for each one based on the file name
# only files existing will show up in web page